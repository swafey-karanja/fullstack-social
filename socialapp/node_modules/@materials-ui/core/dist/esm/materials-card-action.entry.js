import { r as registerInstance } from './index-c0dc768b.js';

const CardAction = class {
    constructor(hostRef) {
        registerInstance(this, hostRef);
    }
    async componentWillLoad() {
        if (!this.icon && !this.label) {
            console.error('You must define an icon or a label');
            return Promise.reject();
        }
        if (this.icon && !this.label) {
            console.warn('You should define a label in addition to the icon');
        }
    }
    render() {
        return;
    }
    static get style() { return ""; }
};

export { CardAction as materials_card_action };
