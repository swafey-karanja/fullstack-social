import { EventEmitter } from '../../../stencil.core';
export declare class DatatableBodyRow {
    host: HTMLElement;
    selectable: boolean;
    dense: boolean;
    selected: boolean;
    expendable: boolean;
    expended: boolean;
    selectRow: EventEmitter<boolean>;
    expand: EventEmitter<boolean>;
    watchSelected(): void;
    handleClick(): void;
    toggleExpendable(e: Event): void;
    render(): any;
}
