import { EventEmitter } from '../../stencil.core';
import { Sort } from './sort';
export declare class Datatable {
    private header;
    private sortColsMap;
    private footer;
    private rows;
    host: HTMLMaterialsDatatableElement;
    /** Set the color scheme for the datatable */
    color: 'primary' | 'secondary';
    /** Apply low density on the element */
    dense: boolean;
    /** Apply odd style to the element  */
    oddEvenStyle: boolean;
    /** Emitted when datatable is sorted  */
    sort: EventEmitter<Sort>;
    componentDidLoad(): void;
    updateChildrenProperties(): void;
    listenSort(event: CustomEvent<Sort>): void;
    render(): any;
}
