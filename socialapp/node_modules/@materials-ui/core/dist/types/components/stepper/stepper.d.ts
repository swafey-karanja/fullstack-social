export declare class Stepper {
    host: HTMLElement;
    currentStep: HTMLMaterialsStepElement;
    showButtons: boolean;
    activeStep: number;
    inactiveSteps: number[];
    componentDidLoad(): void;
    calcIndexes(): Promise<void>;
    handleActiveStepUpdate(): Promise<void>;
    switchStep(ev: any): void;
    openStep(index: number): Promise<void>;
    nextStep(): Promise<void>;
    previousStep(): Promise<void>;
    adjustHeight(): Promise<void>;
    render(): any;
}
