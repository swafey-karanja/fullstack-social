import { SnackBarOptions } from './SnackbarOptions';
export declare class SnackbarController {
    host: HTMLElement;
    snackQueue: HTMLMaterialsSnackbarElement[];
    private controllerEl;
    /**
     * Show a Snackbar
     * @param {SnackBarOptions} options
     */
    create(options: SnackBarOptions): Promise<void>;
    pushToQueue(snack: HTMLMaterialsSnackbarElement): Promise<void>;
    sliceQueue(): Promise<void>;
    handleQueue(newValue: HTMLMaterialsSnackbarElement[], oldValue: HTMLMaterialsSnackbarElement[]): void;
}
