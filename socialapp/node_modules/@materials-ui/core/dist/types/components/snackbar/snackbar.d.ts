import { EventEmitter } from '../../stencil.core';
export declare class Snackbar {
    private snackbarEl;
    private mdcSnackbar;
    /** Message displayed inside the snackbar */
    label: string;
    stacked: boolean;
    leading: boolean;
    dismissButton: boolean;
    actionText?: string;
    timeout: number;
    actionHandler?: Function;
    close: EventEmitter;
    componentDidLoad(): void;
    handleClose(event: any): void;
    getClasses(): {
        'mdc-snackbar': boolean;
        'mdc-snackbar--stacked': boolean;
        'mdc-snackbar--leading': boolean;
    };
    render(): any;
}
