import { EventEmitter } from '../../stencil.core';
export declare class Switch {
    host: HTMLElement;
    change: EventEmitter;
    disabled: boolean;
    label: string;
    checked: boolean;
    private switch;
    componentDidLoad(): void;
    switchUpdate(): void;
    render(): any[];
}
