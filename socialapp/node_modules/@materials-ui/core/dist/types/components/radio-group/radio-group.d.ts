import { EventEmitter } from '../../stencil.core';
export declare class RadioGroup {
    private materialsRadioNodes;
    host: HTMLElement;
    change: EventEmitter;
    name: string;
    linebreak: boolean;
    value: any;
    label: string;
    componentDidLoad(): void;
    handleEvent(event: CustomEvent): void;
    private cleanNeighbors;
    render(): any;
}
